#ifndef WATCHFACE_TERMINAL_H
#define WATCHFACE_TERMINAL_H

#include "main.h"

// Initialize the terminal UI
void load_watchface_terminal();

// Update loop (handles typing animation and battery)
void update_watchface_terminal();

#endif
